<div id="carouselBlk">
                <div id="myCarousel" class="carousel slide">
                    <div class="carousel-inner">

                        <div class="item active">
                            <div class="container">
                                <img style="width:1000px; height:490px" src="{{ asset('assets') }}/themes/images/carousel/1.png" alt="special offers"/>
                                <div class="carousel-caption">
                                    <h4>Second Thumbnail label</h4>
                                    <p></p>
                                </div>
                            </div>
                        </div>

                        {% for rs in sliders %}

                            <div class="item">
                                <div class="container">
                                    <a href="{{ path('product_detail' , {'id':rs.id})  }}">
                                        <img style="width:1000px; height:490px" src="{{ asset('uploads') }}/images/{{ rs.image }}" alt=""/>
                                    </a>
                                    <div class="carousel-caption">
                                        <h4> {{ rs.title }}</h4>
                                        <p></p>
                                    </div>
                                </div>
                            </div>
                        {% endfor %}

                    </div>
                    <a class="left carousel-control" href="#myCarousel" data-slide="prev">&lsaquo;</a>
                    <a class="right carousel-control" href="#myCarousel" data-slide="next">&rsaquo;</a>
                </div>
            </div>
            <div id="mainBody">
                <div class="container">
                    <div class="row">