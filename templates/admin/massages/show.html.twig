{% extends 'base.html.twig' %}

{% block title %}Massages{% endblock %}

{% block body %}
    <h1>Massages</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ massage.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ massage.name }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ massage.email }}</td>
        </tr>
        <tr>
            <th>Subject</th>
            <td>{{ massage.subject }}</td>
        </tr>
        <tr>
            <th>Message</th>
            <td>{{ massage.message }}</td>
        </tr>
        <tr>
            <th>Comment</th>
            <td>{{ massage.comment }}</td>
        </tr>
        <tr>
            <th>Status</th>
            <td>{{ massage.status }}</td>
        </tr>
        </tbody>
    </table>
    <form action="{{ path('admin_massages_update',{'id':massage.id}) }}" method="post">
        <textarea name="comment" cols="20" rows="3">{{ massage.comment }}</textarea>
        <input type="submit" value="Güncelle">
    </form>

    <a href="{{ path('admin_massages_edit', {'id': massage.id}) }}">edit</a>

    {{ include('admin/massages/_delete_form.html.twig') }}
{% endblock %}
